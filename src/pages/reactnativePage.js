const reactnativePage = ()=>{
    const contentPage = document.createElement('section');

    const titulo = document.createElement('h2');
    titulo.textContent = 'Guardar';

    contentPage.appendChild(titulo);
    return contentPage;
}

export default reactnativePage;